<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:telerikInput="clr-namespace:Telerik.XamarinForms.Input;assembly=Telerik.XamarinForms.Input"
             x:Class="SDKBrowser.Examples.EntryControl.FeaturesCategory.ReadOnlyExample.ReadOnly">
  <ContentView.Content>
        <StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="IsReadOnly : "/>
                <Switch IsToggled="{Binding Source={x:Reference telerikEntry}, Path=IsReadOnly}"/>
            </StackLayout>
            <!-- >> entry-features-read-only -->
            <telerikInput:RadEntry x:Name="telerikEntry" Text="Telerik UI for Xamarin Entry control" IsReadOnly="True"/>
            <!-- << entry-features-read-only -->
        </StackLayout>
    </ContentView.Content>
</ContentView>