<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:telerikInput="clr-namespace:Telerik.XamarinForms.Input;assembly=Telerik.XamarinForms.Input"
             x:Class="SDKBrowser.Examples.EntryControl.FeaturesCategory.TextBindingExample.TextBinding">
    <StackLayout Margin="40, 10" Spacing="10">     
        <!-- >> entry-features-textbinding -->
        <telerikInput:RadEntry WatermarkText="Enter sample text"
                               Text="{Binding MyEntryText, Mode=TwoWay}" />
        <!-- << entry-features-textbinding -->
        <StackLayout Orientation="Horizontal">
            <Label Text="You entered: " />
            <Label Text="{Binding MyEntryText}" />
        </StackLayout>
    </StackLayout>
</ContentView>